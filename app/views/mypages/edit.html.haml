= render 'items/shared/loginheader'
- breadcrumb :mypage_edit
= render "mypages/breadcrumbs-header"
.my-contents
  =render 'sidebar'

  .profile-box
    -#form_with...テーブルが完成してから要変更
    = form_with model: @user do |f|
      .profile-box__header
        %h2.profile プロフィール
      .profile-box__image-container
        .profile-box__image-container__user-info
          %figure.user-image
            = image_tag "user_none_img.png", size: "60x60", class: "user-img"
          = f.text_field :nickname, class:'user_name', value: current_user.nickname, placeholder:"例)AYA☆セール中"
      .profile-box__message-container
        = form_with model: @user do |f|
          = f.text_area :name, class:'user-message', placeholder:"例）こんにちは☆ ご覧いただきありがとうございます！かわいいものやきれいめオフィスカジュアル中心に出品しています。服のサイズは主に9号です。気持ちよくお取引できるよう心がけていますので、商品や発送方法などご質問がありましたらお気軽にどうぞ♪"
          = link_to mypages_path, class: "btn-default btn-red" do
            登録する 
= render "items/shared/sellbtn"
= render 'items/shared/footer'

:css
  .header{
    box-shadow: none;
  }
